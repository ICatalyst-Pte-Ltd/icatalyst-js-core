export declare type Version = {
    type: string;
    /**
     * A comma separated value with the component and version string
     * @example my component, v3.4.5(probably a release date)
     */
    value: string;
};
