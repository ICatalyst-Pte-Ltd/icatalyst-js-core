/**
 * Rejects any values that contain dangerous HTML tags or attributes
 * @param value the value to check
 * @returns true if okay, false otherwise
 */
export declare function isHTMLSafe(value: string): boolean;
